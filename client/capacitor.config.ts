import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sreehari.letstalk',
  appName: 'Lets Talk',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
